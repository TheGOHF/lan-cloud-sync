# LAN Cloud Sync: Инвентаризация возможностей

Документ составлен только по фактам, видимым в текущем репозитории. Он не включает абстрактные советы и не приписывает проекту функций, которых нет в коде.

## 1. Уже реализованные возможности системы

### Возможности на стороне сервера

- В `server/app/main.py` реализовано FastAPI server application.
- Реализован HTTP API для получения списка метаданных файлов:
  `GET /files`.
- Реализован HTTP API для загрузки файла:
  `POST /upload`.
- Реализован HTTP API для скачивания файла:
  `GET /download`.
- Реализован HTTP API для удаления файла:
  `DELETE /files`.
- Сервер хранит метаданные файлов в SQLite. Путь по умолчанию: `server/data.db`.
- Сервер хранит физические файлы в storage. Путь по умолчанию: `server/storage/`.
- Серверные пути `db_path` и `storage_path` можно переназначить через
  `%LOCALAPPDATA%\lan-cloud-sync\server-config.json`.
- Сервер вычисляет SHA-256 для загруженных файлов.
- Сервер увеличивает версию файла при изменении содержимого.
- Сервер увеличивает версию файла при удалении.
- Сервер хранит состояние удаления через флаг `deleted`.
- Сервер хранит `device_id` стороны, внесшей последнее изменение.
- Сервер валидирует относительные пути и отклоняет абсолютные пути и `..`.
- Сервер отдаёт файлы потоково по чанкам.
- Сервер удаляет физический файл из storage при удалении.
- Сервер согласует метаданные в БД с фактическим содержимым storage при вызове `GET /files`.
  Это включает:
  создание metadata rows для файлов в storage, которых нет в БД,
  пометку DB rows как deleted при отсутствии физических файлов,
  восстановление удалённых DB rows, если соответствующие файлы снова есть в storage.

### Возможности на стороне клиента

- В `client/app/cli/main.py` реализован CLI client.
- Реализованы CLI-команды:
  `sync`, `status`, `upload`, `download`, `list`, `watch`, `prune-tombstones`.
- В `client/app/gui/main.py` реализован минимальный GUI client.
- Клиент хранит локальное состояние синхронизации в SQLite по пути из
  `%LOCALAPPDATA%\lan-cloud-sync\client-config.json`.
- Клиент рекурсивно сканирует локальную папку синхронизации.
- Клиент вычисляет SHA-256 для локальных файлов.
- Клиент получает `mtime` файла при локальном сканировании.
- Клиент запрашивает удалённые метаданные с сервера.
- Клиент загружает локальные файлы на сервер.
- Клиент скачивает удалённые файлы с сервера.
- Клиент отправляет delete requests на сервер.
- Клиент строит sync plan сравнением:
  локального состояния файловой системы,
  удалённых метаданных,
  локального состояния из БД.
- Клиент поддерживает следующие sync actions:
  `upload`,
  `download`,
  `delete_remote`,
  `delete_local`,
  `mark_local_deleted`,
  `conflict_download`.
- Клиент сохраняет локальное tombstone state с `deleted=True`.
- Клиент обрабатывает конфликт так:
  сохраняет локальную версию как отдельный `_conflict` файл,
  затем скачивает удалённую версию в исходный путь.
- Клиент выполняет начальный sync cycle при старте watcher.
- Клиент следит за изменениями локальной файловой системы через `watchdog`.
- Клиент делает debounce локальных file events через `threading.Timer`.
- Клиент периодически опрашивает сервер в фоне потоке.
- Клиент предотвращает перекрывающиеся sync cycles через `Lock`.

### Операционные и вспомогательные возможности

- В `scripts/` есть start scripts для сервера и client watcher.
- В `scripts/` есть stop scripts для сервера и client watcher.
- В `scripts/check_status.bat` есть status-check script.
- В `scripts/` есть scripts для установки и удаления Windows autostart tasks.
- В `scripts/` есть scripts для очистки tombstone-записей клиента и сервера.
- Логи пишутся через script-level stdout/stderr redirection в `logs/`.

## 2. Что отсутствует или не завершено по текущему репозиторию

Ниже перечислены только пункты, напрямую подтверждённые кодом или TODO comments.

### Возможности, явно не завершённые в коде

- Настоящий delta sync не реализован.
  Подтверждение:
  в `client/app/sync/network.py` есть `updated_since`,
  но серверный `GET /files` его не принимает и не обрабатывает.
- Не реализована pagination для server file listing.
  Подтверждение:
  TODO в `server/app/services/file_service.py`.
- Не реализована server-side фильтрация по updated-since при выдаче списка файлов.
  Подтверждение:
  TODO в `server/app/services/file_service.py`.
- Не реализовано отдельное хранение server delta checkpoints.
  Подтверждение:
  TODO в `client/app/sync/sync_engine.py`.
- Не реализовано выделение conflict policy в отдельный механизм.
  Подтверждение:
  TODO в `client/app/sync/sync_engine.py`.
- Не реализованы дополнительные multi-device merge rules.
  Подтверждение:
  TODO в `client/app/sync/sync_engine.py`.
- Не реализовано вычисление hash во время потоковой записи upload на сервере.
  Подтверждение:
  TODO в `server/app/services/storage_service.py`;
  сейчас hash вычисляется после того, как файл уже записан.

### Возможности, не найденные в репозитории

- В репозитории отсутствует web frontend.
- Не найден WebSocket или другой push-based канал удалённых обновлений.
- В server routes не найден слой authentication или authorization.
- Не найдена user/account model.
- В дереве репозитория не найден явный test suite.
- Не найдена `.env`-конфигурация.

### Замечание по объёму вывода

- Список отсутствующего ограничен только тем, что можно проверить напрямую по репозиторию.
- Он не утверждает, что все эти функции обязательны для любого формата курсового проекта.

## 3. Нужен ли минимальный GUI для защиты

### Оценка по фактам

- Текущий репозиторий предоставляет рабочую поверхность взаимодействия через CLI-команды, GUI и start/stop/status scripts.
- В текущем репозитории есть минимальная GUI-реализация в `client/app/gui/main.py`.
- Поведение системы, которое можно продемонстрировать по коду, включает:
  запуск сервера,
  настройку клиента через GUI,
  запуск client watcher,
  обнаружение локальных изменений файлов,
  upload/download/delete поведение,
  создание conflict copy,
  вывод локального sync-state состояния.

### Вывод только по репозиторию

- Минимальный GUI технически не обязателен для фоновой работы, потому что постоянный режим лучше запускать через watcher.
- GUI полезен для первичной настройки папки синхронизации, адреса сервера и ручной синхронизации.
- Если формат защиты требует визуального взаимодействия, текущий репозиторий такой слой уже предоставляет.

### Ограничения текущего GUI

- GUI не является системным tray-приложением.
- GUI не устанавливает автозапуск сам; для этого используются scripts.
- GUI не содержит отдельного экрана обслуживания tombstone-записей.
